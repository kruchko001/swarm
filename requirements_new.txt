# Training-only dependencies for local agent development.
# Excludes validator/backend/packaging dependencies from requirements.txt.
#
# CUDA notes:
# - This file targets NVIDIA GPU training (CUDA 12.8 wheels).
# - Requires NVIDIA driver already installed on host.
--extra-index-url https://download.pytorch.org/whl/cu128

# Core ML / RL stack
numpy>=2.0.1,<3.0.0
torch==2.11.0+cu128
stable-baselines3[extra]==2.7.1
gymnasium==0.29.1
onnxruntime-gpu==1.23.2

# Swarm simulation environment stack
swarm-bullet3==2.0.0.1
swarm-drone-gym==2.0.0.1

# Needed by swarm.protocol used during task/env setup
msgpack==1.1.2
# gym_pybullet_drones still imports pkg_resources
setuptools<82
